# headr - Rustによる `head` コマンドの実装

本書は `command-line-rust` の学習用プロジェクトの一部であり、Unix 標準の `head` コマンドを Rust で再実装したものです。

## 目次 (Table of Contents)

- [headr - Rustによる `head` コマンドの実装](#headr---rustによる-head-コマンドの実装)
  - [目次 (Table of Contents)](#目次-table-of-contents)
  - [機能](#機能)
  - [使用方法](#使用方法)
  - [UTF-8 の扱い](#utf-8-の扱い)
  - [ファイル構成](#ファイル構成)
    - [モジュール構成](#モジュール構成)
  - [開発](#開発)
    - [Lint (静的解析)](#lint-静的解析)
    - [テストとカバレッジ](#テストとカバレッジ)

## 機能

- 先頭からの行数指定出力 (`-n`, `--lines`)
- 先頭からのバイト数指定出力 (`-c`, `--bytes`)
- 複数ファイルの処理
- 標準入力のサポート

## 使用方法

```bash
# ビルドして実行
cargo run -- [オプション] [ファイル...]

# 例: ファイルの先頭 10 行を表示 (デフォルト)
cargo run -- filename.txt

# 例: 先頭 5 行を表示
cargo run -- -n 5 filename.txt

# 例: 先頭 20 バイトを表示
cargo run -- -c 20 filename.txt
```

## UTF-8 の扱い

本実装では、バイナリとしてファイルを読み込んだ後、`String::from_utf8_lossy` を使用して文字列に変換して出力します。
そのため、マルチバイト文字（UTF-8）の途中でバイト数が制限された場合（`-c` オプション使用時など）、不正なバイトシーケンスは Unicode 置換文字 (�) に置き換えられます。プログラムがパニックすることはありません。

**例:**
`α` (2バイト) + `あ` (3バイト) というファイルの場合:

- `-c 1` (1バイト目のみ): `�` (α の前半のみ読み込まれるため置換)
- `-c 3` (3バイト目まで): `α�` (あ の1バイト目のみ読み込まれるため置換)

## ファイル構成

プロジェクトの主要なファイル構成は以下の通りです。

```text
.
├── Cargo.toml          # プロジェクト設定と依存関係
├── src
│   ├── lib.rs          # ライブラリ: Args, run, open, DEFAULT_LINES
│   └── main.rs         # エントリポイント (lib.rs を呼び出す薄いラッパー)
└── tests
    ├── cli.rs          # 統合テスト (コマンドライン引数のテスト)
    ├── inputs          # テスト入力用ファイル
    └── expected        # テストの期待される出力結果
```

### モジュール構成

| ファイル | 説明 |
|----------|------|
| `src/lib.rs` | コアロジック。`Args` 構造体、`run` 関数、`open` 関数、`DEFAULT_LINES` 定数を含む。ユニットテスト3件。 |
| `src/main.rs` | 薄いエントリポイント。`lib.rs` を呼び出すのみ（9行）。 |
| `tests/cli.rs` | 統合テスト57件。エラーケース、複数ファイル、stdin テストを含む。 |

## 開発

### Lint (静的解析)

コードの品質を保つために `cargo clippy` を使用しています。

```bash
cargo clippy
```

より厳格なチェックを行う場合は以下を実行します（推奨）:

```bash
cargo clippy -- -W clippy::pedantic
```

### テストとカバレッジ

本プロジェクトには、ユニットテストと統合テストの両方が含まれています。

**テストの実行:**

```bash
cargo test
```

**テスト結果:**

- ユニットテスト: 3件
- 統合テスト: 57件
- 合計: **60件**

**カバレッジレポートの生成:**

`cargo-llvm-cov` を使用してカバレッジを計測できます（推奨）:

```bash
# インストール
cargo install cargo-llvm-cov

# カバレッジ計測
cargo llvm-cov

# HTML レポート生成
cargo llvm-cov --html
```

**現在のカバレッジ:**

| 指標 | カバレッジ |
|------|------------|
| Lines | **100%** |
| Functions | **100%** |
| Regions | 97.78% |
